image: mcr.microsoft.com/playwright:v1.62.1-jammy

playwright_tests:
  script:
    - npm ci
    - npx playwright test --reporter=html

  artifacts:
    when: always
    paths:
      - playwright-report/
      - test-results/
    expire_in: 2 days
